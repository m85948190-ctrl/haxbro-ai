(()=>{
const $=id=>document.getElementById(id);
function addButton(){const pub=$('appMakerPublish');if(!pub||$('appMakerExport'))return;const b=document.createElement('button');b.id='appMakerExport';b.className='ghost';b.type='button';b.textContent='⬇ DOWNLOAD APP';b.style.marginTop='10px';pub.parentNode?.appendChild(b);b.addEventListener('click',downloadApp)}
function addVisitButton(){const box=$('appMakerPublishedUrl'),input=$('appMakerUrl');if(!box||!input||$('appMakerVisit'))return;const b=document.createElement('button');b.id='appMakerVisit';b.className='kai-refresh';b.type='button';b.textContent='↗ VISIT PUBLIC APP';b.style.marginTop='8px';box.appendChild(b);b.addEventListener('click',()=>{if(input.value)window.open(input.value,'_blank','noopener')})}
function downloadApp(){const iframe=$('appMakerPreview'),status=$('appMakerStatus');let html='';try{html=iframe?.contentDocument?.documentElement?.outerHTML||''}catch{}if(!html){status.textContent='Build the app first, then download it.';return}const blob=new Blob(['<!doctype html>\n'+html],{type:'text/html;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=(($('appMakerPrompt')?.value||'haxbro-app').trim().slice(0,40).replace(/[^a-z0-9]+/gi,'-').replace(/^-|-$/g,'').toLowerCase()||'haxbro-app')+'.html';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),2000);status.textContent='✅ App downloaded.'}
function init(){addButton();addVisitButton();new MutationObserver(()=>{addButton();addVisitButton()}).observe(document.documentElement,{subtree:true,childList:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();