(() => {
  const $ = id => document.getElementById(id);
  let duration = 15;
  function open(){const p=$('reelCreatorPanel');if(p){p.hidden=false;p.scrollIntoView({behavior:'smooth',block:'center'});$('reelTopic')?.focus();}}
  function close(){$('reelCreatorPanel')?.setAttribute('hidden','');}
  ['reelCreatorBtn','mobileReelCreatorBtn'].forEach(id=>$(id)?.addEventListener('click',open));
  $('reelCreatorClose')?.addEventListener('click',close);
  document.querySelectorAll('#reelDurations button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('#reelDurations button').forEach(x=>x.classList.remove('active'));b.classList.add('active');duration=Number(b.dataset.duration||15);}));
  $('reelCreate')?.addEventListener('click',async()=>{const idea=($('reelTopic')?.value||'').trim(),s=$('reelStatus'),o=$('reelOutput');if(!idea){s.textContent='Enter your reel idea first.';return;}s.textContent='Creating reel plan…';try{const r=await fetch('/api/reel-plan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({idea,duration})});const d=await r.json();if(!r.ok)throw Error(d.error||'Could not create reel');o.textContent=d.plan||'';s.textContent=`✅ Reel plan ready · ${d.duration}s`;}catch(e){s.textContent=`Reel plan failed: ${e.message}`;}});
  $('reelGeneratePreview')?.addEventListener('click',async()=>{const idea=($('reelTopic')?.value||'').trim(),s=$('reelStatus');if(!idea){s.textContent='Enter your reel idea first.';return;}s.textContent='🎬 Generating the actual reel video… please wait.';try{const r=await fetch('/api/skeleton-video',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({prompt:idea})});const d=await r.json();if(!r.ok||!d.videoUrl)throw Error(d.error||'Video generation failed');let v=$('reelVideo');if(!v){v=document.createElement('video');v.id='reelVideo';v.controls=true;v.playsInline=true;v.className='reel-video';$('reelOutput')?.after(v);}v.src=d.videoUrl;v.load();s.textContent='✅ Actual reel generated. Play it below.';v.scrollIntoView({behavior:'smooth',block:'center'});}catch(e){s.textContent=`Video generation failed: ${e.message}`;}});
})();