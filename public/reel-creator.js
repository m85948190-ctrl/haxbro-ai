(()=>{const $=s=>document.querySelector(s);const panel=$('#reelCreatorPanel'),topic=$('#reelTopic'),output=$('#reelOutput'),status=$('#reelStatus');if(!panel)return;$('#reelCreatorBtn')?.addEventListener('click',()=>{panel.hidden=false;topic?.focus()});$('#mobileReelCreatorBtn')?.addEventListener('click',()=>{panel.hidden=false;topic?.focus();$('#mobileDrawer')?.classList.remove('open')});$('#reelCreatorClose')?.addEventListener('click',()=>{panel.hidden=true});let duration=15;document.querySelectorAll('#reelDurations button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('#reelDurations button').forEach(x=>x.classList.remove('active'));b.classList.add('active');duration=Number(b.dataset.duration)}));$('#reelCreate')?.addEventListener('click',async()=>{const idea=topic.value.trim()||'Create a professional HAxBRO Instagram Reel using the skeleton host concept and the real HAxBRO product identity.';status.textContent='HAxBRO is creating your reel plan…';output.textContent='Generating storyboard…';try{const r=await fetch('/api/reel-plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({idea,duration})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Reel planner failed');output.textContent=d.plan||JSON.stringify(d,null,2);status.textContent=`Reel plan ready · ${duration>=60?Math.floor(duration/60)+' min':duration+' sec'} · ${d.scenes||Math.max(1,Math.ceil(duration/5))} scenes`}catch(e){output.textContent='Unable to create plan. '+(e.message||'Try again.');status.textContent='Reel Creator error.'}});$('#reelGeneratePreview')?.addEventListener('click',async()=>{status.textContent='Generating the 10-second preview…';try{const r=await fetch('/api/skeleton-video',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt:topic.value.trim()||'Introduce HAxBRO with the professional skeleton host concept.'})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Video generation failed');output.textContent='Preview ready:\n'+d.videoUrl;status.textContent='10-second preview generated.';window.open(d.videoUrl,'_blank')}catch(e){status.textContent=e.message||'Preview generation failed.'}});})();