(()=>{
'use strict';
const KEY='haxbro_theme';
const root=()=>document.body;
function applyTheme(theme){const light=theme==='light';root().classList.toggle('light-theme',light);root().dataset.theme=light?'light':'dark';const btn=document.getElementById('themeBtn');const mobile=document.getElementById('mobileThemeBtn');if(btn)btn.textContent=light?'☾ DARK':'☀ LIGHT';if(mobile)mobile.textContent=light?'☾ DARK MODE':'☀ LIGHT MODE';try{localStorage.setItem(KEY,light?'light':'dark')}catch{} }
function toggleTheme(){applyTheme(root().classList.contains('light-theme')?'dark':'light')}
function initTheme(){let saved='dark';try{saved=localStorage.getItem(KEY)||'dark'}catch{}applyTheme(saved);document.getElementById('themeBtn')?.addEventListener('click',toggleTheme);document.getElementById('mobileThemeBtn')?.addEventListener('click',()=>{toggleTheme();closeDrawer()});}
let backdrop;
function openDrawer(){const d=document.getElementById('mobileDrawer');if(!d)return;d.classList.add('open');d.setAttribute('aria-hidden','false');document.documentElement.classList.add('drawer-open');if(!backdrop){backdrop=document.createElement('div');backdrop.className='mobile-drawer-backdrop';backdrop.addEventListener('click',closeDrawer);document.body.appendChild(backdrop)}backdrop.hidden=false;requestAnimationFrame(()=>backdrop.classList.add('open'));}
function closeDrawer(){const d=document.getElementById('mobileDrawer');if(!d)return;d.classList.remove('open');d.setAttribute('aria-hidden','true');document.documentElement.classList.remove('drawer-open');if(backdrop){backdrop.classList.remove('open');setTimeout(()=>{if(backdrop)backdrop.hidden=true},180)}}
function initDrawer(){document.getElementById('menuBtn')?.addEventListener('click',openDrawer);document.getElementById('closeMenuBtn')?.addEventListener('click',closeDrawer);document.addEventListener('keydown',e=>{if(e.key==='Escape')closeDrawer()});}
function initVoice(){const b=document.getElementById('micBtn'),input=document.getElementById('promptInput');if(!b||!input)return;const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){b.title='Voice input is not supported in this browser';return}let rec=null,active=false;const set=(v)=>{active=v;b.classList.toggle('recording',v);b.setAttribute('aria-label',v?'Stop voice input':'Start voice input');b.title=v?'Stop voice input':'Start voice input'};b.addEventListener('click',()=>{if(active){try{rec?.stop()}catch{}set(false);return}rec=new SR();rec.lang=navigator.language||'en-US';rec.interimResults=true;rec.continuous=false;rec.onstart=()=>set(true);rec.onend=()=>set(false);rec.onerror=()=>set(false);rec.onresult=e=>{let text='';for(let i=e.resultIndex;i<e.results.length;i++)text+=e.results[i][0].transcript;input.value=text.trim();input.dispatchEvent(new Event('input',{bubbles:true}))};try{rec.start()}catch{set(false)}})}
function addRealMakerEntry(){const links=document.querySelectorAll('a.maker-link,button.maker-link');if([...links].some(x=>/real maker/i.test(x.textContent||'')))return;const host=document.querySelector('.ai-tools.normal-admin-hidden');if(!host)return;const b=document.createElement('button');b.className='maker-link';b.type='button';b.textContent='✦ Real Maker';b.addEventListener('click',()=>window.open('real-maker.html','_blank','noopener'));host.appendChild(b)}
function init(){initTheme();initDrawer();initVoice();addRealMakerEntry()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
window.HAxBROWorkspace={applyTheme,toggleTheme,openDrawer,closeDrawer};
})();
